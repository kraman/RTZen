module demo
{
	module serial_transport
	{
        struct WayPoint
        {
            long latitude;
            long longitude;
            short altitude;
        };

        typedef WayPoint NoFlyZone[4];

		interface GroundStation
		{
            oneway void sendWayPoint(in short id, in NoFlyZone noFlyZone);
        };
	};
};
